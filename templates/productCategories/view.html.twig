<a href="#" class="card text-white shadow">
    <img class="card-img" src="{{ productCategory.image }}" alt="Card image">
    <div class="card-img-overlay card-body">
        <h5 class="card-title">{{ productCategory.name }}</h5>
        <p class="card-text">{{ productCategory.description }}</p>
    </div>
</a>
